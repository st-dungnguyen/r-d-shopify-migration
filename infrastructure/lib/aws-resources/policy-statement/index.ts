// import { PolicyStatement } from 'aws-cdk-lib/aws-iam';

// You can create custom policy statements such as sending mail from AWS SES policy
// Example:
// const sesPolicy = new PolicyStatement({
//   actions: ['ses:SendEmail', 'ses:SendRawEmail', 'ses:SendTemplatedEmail'],
//   resources: ['*'],
// });

// export const customPolicies: PolicyStatement[] = [sesPolicy];
